(Program) {
    .items = [
        [0] = Func((FuncDef) {
            .return_type = void,
            .ident = "main",
            .params = [],
            .statements = [
                [0] = Print(Expr(BinaryOp((BinOpExpr) {
                    .op = LogAnd,
                    .lhs = Literal(Int(1)),
                    .rhs = Literal(Int(1))
                })))
            ]
        })
    ]
}
