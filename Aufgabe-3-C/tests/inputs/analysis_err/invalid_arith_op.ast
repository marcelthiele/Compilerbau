(Program) {
    .items = [
        [0] = Func((FuncDef) {
            .return_type = void,
            .ident = "main",
            .params = [],
            .statements = [
                [0] = Print(Expr(BinaryOp((BinOpExpr) {
                    .op = Add,
                    .lhs = Literal(Bool(true)),
                    .rhs = Literal(Bool(true))
                })))
            ]
        })
    ]
}
