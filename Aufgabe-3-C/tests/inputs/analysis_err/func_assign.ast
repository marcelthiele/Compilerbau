(Program) {
    .items = [
        [0] = Func((FuncDef) {
            .return_type = void,
            .ident = "main",
            .params = [],
            .statements = [
                [0] = Assign((Assign) {
                    .lhs = (ResIdent) {
                        .ident = "main"
                    },
                    .rhs = Call((FuncCall) {
                        .res_ident = (ResIdent) {
                            .ident = "main"
                        },
                        .args = []
                    })
                })
            ]
        })
    ]
}
