void main() {
    bool main;
    { bool main; }
    // `for`-loops create a new scope, but `if`/`while` do not
    for (bool main; false; main = false)
        ;
    for (main = false; false; main = false)
        int main;
}
void bar(int main) {}
