(Program) {
    .items = [
        [0] = Func((FuncDef) {
            .return_type = void,
            .ident = "main",
            .params = [],
            .statements = [
                [0] = VarDef((VarDef) {
                    .data_type = int,
                    .res_ident = (ResIdent) {
                        .ident = "x"
                    },
                    .init = BinaryOp((BinOpExpr) {
                        .op = Div,
                        .lhs = Literal(Int(1)),
                        .rhs = Literal(Int(0))
                    })
                })
            ]
        })
    ]
}
